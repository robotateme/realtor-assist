Текущий JSON:
{current_json}

Сообщение клиента:
"{{$clientMessage}}"

Сделай:

1. Извлеки новые факты из сообщения
2. Добавь их в facts (без дублей)
3. Обнови case_type и issue если понятно
4. Определи текущий этап

ЛОГИКА:

ЕСЛИ facts < 6:
→ продолжай сбор
→ задай 1 уточняющий вопрос

ЕСЛИ facts >= 6 И confirmation.asked = false:
→ сформируй summary (1-2 предложения)
→ задай вопрос подтверждения
→ stage = confirm

ЕСЛИ confirmation.asked = true И confirmed = null:
→ определи:
- если пользователь согласен → confirmed = true
- если исправляет → обнови facts и summary

ЕСЛИ confirmed = true:
→ переход к сбору контакта
→ stage = collect_contact

ЕСЛИ контакт заполнен:
→ stage = done

ОГРАНИЧЕНИЯ:

- максимум 1 вопрос
- не повторяй вопросы
- не уходи в лишние детали
- не спрашивай контакт раньше времени

---

ФОРМАТ ответа строго JSON (без текста вне JSON)
